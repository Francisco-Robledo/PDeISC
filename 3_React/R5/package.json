{
  "name": "sistema_usuarios_r5",
  "version": "1.0.0",
  "description": "Sistema de Usuarios - R5 (Google OAuth + BBDD SQL 3FN + React)",
  "scripts": {
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
